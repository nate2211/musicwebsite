import { useEffect, useState } from "react";
export function useMidi(onNote){const [status,setStatus]=useState("MIDI unavailable");useEffect(()=>{if(!navigator.requestMIDIAccess){setStatus("MIDI unsupported");return;}let access;const handle=(event)=>{const [cmd,note,velocity]=event.data;if((cmd&0xf0)===0x90&&velocity>0)onNote?.(note,velocity/127);};navigator.requestMIDIAccess().then(a=>{access=a;setStatus(`${a.inputs.size} MIDI input${a.inputs.size===1?"":"s"}`);a.inputs.forEach(input=>input.addEventListener("midimessage",handle));}).catch(()=>setStatus("MIDI permission denied"));return()=>access?.inputs.forEach(input=>input.removeEventListener("midimessage",handle));},[onNote]);return status;}
