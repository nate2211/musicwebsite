import { useEffect } from "react";
export function useKeyboardShortcuts({onPlayPause,onStop,onSave,onUndo,onRedo,setView}){useEffect(()=>{const handler=(event)=>{if(["INPUT","TEXTAREA","SELECT"].includes(document.activeElement?.tagName))return; if(event.code==="Space"){event.preventDefault();onPlayPause();} if(event.code==="Escape")onStop(); if((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==="s"){event.preventDefault();onSave();} if((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==="z"){event.preventDefault();event.shiftKey?onRedo?.():onUndo?.();} if(event.key==="1")setView("rack");if(event.key==="2")setView("piano");if(event.key==="3")setView("playlist");if(event.key==="4")setView("mixer");};window.addEventListener("keydown",handler);return()=>window.removeEventListener("keydown",handler);},[onPlayPause,onStop,onSave,onUndo,onRedo,setView]);}
