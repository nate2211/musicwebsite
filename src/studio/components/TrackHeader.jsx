import React, { useState } from "react";
export function TrackHeader({track,selected,onSelect,onUpdate,onDuplicate,onDelete,onClear}){const [menu,setMenu]=useState(false);return <div className={`track-header ${selected?"selected":""}`} onClick={onSelect}><span className="track-color" style={{background:track.color}}/><input value={track.name} onClick={e=>e.stopPropagation()} onChange={e=>onUpdate({name:e.target.value})}/><button className={track.mute?"active mute":""} onClick={e=>{e.stopPropagation();onUpdate({mute:!track.mute})}}>M</button><button className={track.solo?"active solo":""} onClick={e=>{e.stopPropagation();onUpdate({solo:!track.solo})}}>S</button><button onClick={e=>{e.stopPropagation();setMenu(v=>!v)}}>•••</button>{menu&&<div className="track-menu"><button onClick={onDuplicate}>Duplicate</button><button onClick={onClear}>Clear pattern</button><button onClick={onDelete}>Delete track</button></div>}</div>}
